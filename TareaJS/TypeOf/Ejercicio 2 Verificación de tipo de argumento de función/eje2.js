// Ejercicio 2: Verificación de tipo de argumento de función
// En este ejercicio, se define una función que recibe un argumento y 
// se muestra en la consola el tipo de dato utilizando el operador typeof.

function tipo(arg){
    console.log("El argumento de la funcion es de tipo:"+typeof(arg));
}

let arg= 10;
tipo(arg);